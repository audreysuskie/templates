//- mixin reviews (review)
//-   .reviews__card
//-     .reviews__clientPhoto
//-       img.reviews__client-img(src=`/images/reviews/photo1.png`, alt=`Photo of Client`)
//-       h6.reviews__user Jane Doe
//-     p.reviews__text Absolutely the best treatment for wrinkles. No side effects and lasts a long time. Would highly recommend!
//-     .reviews__rating
//-       each star in [1, 2, 3, 4, 5]
//-         svg.reviews__star(class=`reviews__${star}--5`)
//-           use(xlink:href='/images/icons.svg#icon-star')

mixin reviews (review)
  .reviews__card
    .reviews__clientPhoto
      img.reviews__client-img(src=`/images/reviews/${review.user.photo}`, alt=`${review.user.name}`)
      .reviews-group
        .reviews-title.reviews__user= review.user.name
        .reviews__text= `${review.service} client`
    p.reviews__text= `"${review.review}"`
    .reviews__rating
      each star in [1, 2, 3, 4, 5]
        svg.reviews__star(class=`reviews__star--${review.rating >= star ? 'active' : 'inactive'}`)
          use(xlink:href='/images/icons.svg#icon-star')